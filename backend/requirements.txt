# Production runtime deps (lean — no torch). In production, embeddings run via the
# Gemini API, so the container never needs sentence-transformers/torch. For the
# local semantic stack or the RAGAS eval, additionally install requirements-local.txt.

# --- Core API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
email-validator==2.2.0
pydantic-settings==2.7.1
python-dotenv==1.0.1
httpx==0.28.1
python-multipart==0.0.12

# --- Orchestration / agents (Phase 3) ---
langgraph==0.2.60
langchain-core==0.3.28

# --- LLM providers (Phase 3) ---
google-genai==2.11.0
PyJWT==2.10.1
groq==0.13.1
stripe==11.3.0

# --- Retrieval / storage (Phase 1–2) ---
numpy==2.2.1
rank-bm25==0.2.2
supabase==2.11.0
pgvector==0.3.6
psycopg[binary]==3.2.3
psycopg-pool==3.2.4

# --- Ingestion (Phase 1) ---
pypdf==5.1.0
sec-edgar-downloader==5.0.3
yfinance==0.2.51
beautifulsoup4==4.12.3
lxml==5.3.0

# --- Graph (Phase 4) ---
networkx==3.4.2

# --- Ops / observability (Phase 12, guarded) ---
redis==5.2.1
rq==2.0.0
sentry-sdk==2.19.2
