# Optional: local semantic embedding/rerank stack + RAGAS evaluation.
# Pulls in torch (large). Not needed in production (Gemini embeddings via API).
#   pip install -r requirements.txt -r requirements-local.txt
sentence-transformers==3.3.1
ragas==0.2.9
datasets==3.2.0
langfuse==2.53.9
